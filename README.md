# MSCS532_Project_Group-4
# Wanderwise

Minimal scaffold for the Wanderwise project.

Structure:

- `src/wanderwise/` — package code
- `tests/` — pytest tests

Run tests:

```bash
python -m pytest wanderwise/tests
```
# Wanderwise

Minimal scaffold for the Wanderwise project.

Structure:

- `src/wanderwise/` — package code
- `tests/` — pytest tests

Run tests:

```bash
python -m pytest wanderwise/tests
```